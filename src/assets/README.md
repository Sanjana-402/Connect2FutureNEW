Drop final brand photography, logos, icons, and font files into the matching
subfolder here, then swap the corresponding URL in `src/utils/images.js`
(for photography) or import directly (for logos/icons/fonts). The site
currently references Unsplash placeholder imagery so it runs immediately
without any local files.
